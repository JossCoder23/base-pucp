version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres_pucp
    restart: always
    environment:
      POSTGRES_USER: user_admin
      POSTGRES_PASSWORD: c0ntr4s3n4
      POSTGRES_DB: dni_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: